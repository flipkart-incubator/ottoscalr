apiVersion: v1
kind: Service
metadata:
  name: {{ include "ottoscalr.fullname" . }}
  labels:
    {{- include "ottoscalr.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "ottoscalr.selectorLabels" . | nindent 4 }}
